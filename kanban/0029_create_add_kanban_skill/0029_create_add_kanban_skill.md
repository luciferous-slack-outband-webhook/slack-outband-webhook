# `/add-kanban` スキルを追加
## 要望
`kanban/` ディレクトリにマークダウンファイルを追加するスキルを作成して欲しい。

また自動的に番号を採番して欲しい。

引数としてタイトルは想定するが、必須とはしない。

ファイル名で使用するタイトルは英語、ファイル内のタイトルは日本語にして欲しい。

要望と目的を必須ではないが、受け取るようにして欲しい。
またタイトルを指定していない場合は、考えてつけて欲しい。

また、マークダウンファイル作成後、 `/kanban` を実行するかを聞いて欲しい。

## 目的
Claude Codeのインタラクティブセッションでカンバンを追加できるようにしたい。
毎回インタラクティブセッションを抜けてから `add_kanban.sh` するのは面倒なので。

## プラン

- `.claude/skills/add-kanban/SKILL.md` を新規作成する
- スキルの動作:
  1. `args` を自由形式テキストとして解釈し、英語タイトル・日本語タイトル・要望・目的を抽出（すべて任意）
  2. Glob で `kanban/[0-9][0-9][0-9][0-9]_*.md` を取得し最大番号 +1 を4桁0パディングで採番
  3. Write ツールで `# タイトル` `## 目的` `## 要望` のテンプレートでファイル生成
  4. 作成後 AskUserQuestion で `/kanban` 実行するか確認し、Yes なら Skill で起動
- `bin/add_kanban.sh` は変更しない（CLI 用途で残す）

---

## 完了サマリー

- **完了日時**: 2026-04-19T17:31:59+09:00
- **対応内容**:
  - `.claude/skills/add-kanban/SKILL.md` を新規作成
  - スキルは Glob で採番 → Write でファイル生成 → AskUserQuestion で `/kanban` 実行確認 → Skill で起動の流れを実装
- **変更ファイル**:
  - `.claude/skills/add-kanban/SKILL.md`
